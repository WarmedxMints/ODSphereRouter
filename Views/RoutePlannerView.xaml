<UserControl x:Class="ODSphereRouter.Views.RoutePlannerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:ODSphereRouter.Controls"
             xmlns:local="clr-namespace:ODSphereRouter.Views"
             xmlns:viewmodels="clr-namespace:ODSphereRouter.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:RoutePlannerViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="20"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <controls:SphereListControl Grid.Column="0"/>

        <controls:RouteListControl Grid.Column="2" />
    </Grid>
</UserControl>
